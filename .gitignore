# === Secrets — NEVER commit ===
.env
.env.local
.env.*.local
client_secret_*.json
*.pem
*.key

# === Build artifacts ===
dist
dist-ssr
dev-dist
*.tsbuildinfo

# === Dependencies ===
node_modules
.pnp
.pnp.js

# === Python / external projects (not part of the Vite app) ===
.venv/*
venv/
__pycache__/
*.py[cod]
litellm_config.yaml
strip_proxy.py
utiliser_litellm

# === Claude local config (private) ===
.claude/settings.local.json
.claude/settings.local.json_*
.claude/settings.json_backup

# === Logs ===
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Editor / OS ===
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
Thumbs.db
bash.exe.stackdump

# === Local npm config (mirror/strict-ssl — personal setup) ===
.npmrc
